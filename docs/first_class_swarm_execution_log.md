# First-Class Swarm — Build Execution Log

Frozen spec: `docs/first_class_swarm_production_plan_2026-07-09.md` (v1.3 FINAL).
This log is the durable record of the M0–M5 + BT2-A build: per-milestone branch, batch decomposition, design decisions taken during implementation, test/review outcomes, and any deviation from the plan (deviations reopen the plan per §10).

Baseline (2026-07-09, commit `f1d1638`): 35/35 tests green, all 14 quality gates green, plan committed to `main`.

Milestone sequencing: M0 → M1 → M2 → {M3a ∥ M3b ∥ M3c} → M4 → M5 → BT2 Stage A.
Delivery: one branch per milestone (`milestone/<id>-<slug>`), red/green-tested before merge to `main`; high-stakes surfaces (gate sandboxing, git-destructive-op guards, historical-record remediation, security preflight) implemented and reviewed inline by the owner model; bounded implementation batches delegated to the Codex workhorse with full diff review.

---

## BT2 Stage A — rehearsal (branch `milestone/bt2a-rehearsal` off merged main containing M0–M5B) — the FINAL milestone of the plan

Scope: plan §10 lines 444–446 + §9.3 + §9.5. Stage A = *rehearsal only* (Stage B, a real fresh research program, is explicitly OUT OF SCOPE and budgeted in its own contract). Owner-implemented inline (tripwire: the pass/fail bar is a protocol invariant; the rehearsal is the integrity-measurement machinery). Builds on the M5-B drill harness + M4 reproduce/registry/release-perimeter machinery.

- **What was built.** `scripts/bt2a_rehearsal.py` / `make bt2a-rehearsal` — a DETERMINISTIC, offline STR frozen-reference regression that asserts the KNOWN ANSWERS reproduce (`reproduce_analysis` byte + content identity on tables/paper_values/exhibits-manifest/disclosure/replication-README+manifest and content-equivalence on the figure sidecars; `render_paper` computed-paper key resolution), executes the seeded-defect drill rotation (the M5-B `ROTATION` — which already spans the empirical fabrication classes `fabricated_run_manifest`/`fabricated_citation`/`fabricated_claim_number`, a proof defect `false_lemma`, AND the required bridge-layer defect `tampered_instance` = a stale `source_processed_manifests` hash → `content_binding_sha256_mismatch`), and exercises the release perimeter (`release_assembly.check_release_integrity` → the all-failing STR registry correctly BLOCKS a release, RECORDED as the expected known outcome, not a rehearsal failure). It writes the committed, deterministic `reports/status/bt2a/rehearsal.json` (verdict + machinery coverage + per-drill catch + measured catch rate + hypothesis ratio + release-perimeter outcome; no timestamp → idempotent) and emits a `bt2a_rehearsal` KPI event to the EPHEMERAL drill journal (never the compliance journal). A drift in any frozen-reference value, a missed injection (raises `MissedInjection`), or a release that fails to block is a RED regression.
- **The pre-committed BT2 pass/fail bar** (`contracts/bt2_bar.json`, §10): `seeded_defect_catch_rate_min: 1.0` (drill baseline: 5/5 caught; unforgivable fabrication class), `human_review_hours_per_artifact_max: 8.0` (§11 ~6h manuscript gate + §5.4 touchpoints), `token_dollar_ceiling_usd: 1500` (§9.1 Stage-A ≈ few-hundred $; Stage-B re-confirmed in its own contract), `unresolved_fabrication_findings_max: 0`, `registered_vs_reported_hypothesis_ratio: 1`; each number carries a `number_provenance` note; plus the Stage B abort clause (four triggers — budget breach / uncaught provenance defect / data-validity break / owner withdrawal — with the required artifact: event-journal closure + a written negative-result report through the manuscript machinery). A new `bt2_bar` gate (57th gate; added to `_MODE_INDEPENDENT_SCIENCE_GATES` + `_ALL_GATE_NAMES` + the runbook registry so `runbook_staleness` now validates 57 gates; SKIPS on a pack with no committed bar) validates the bar is present + well-formed with the abort clause AND holds the LATEST rehearsal report to it: catch rate ≥ `seeded_defect_catch_rate_min`, hypothesis ratio = the committed invariant, release perimeter blocked, frozen reference reproduced. A below-threshold-catch-rate fixture fails the gate (proven in `test_golden_bt2a.py`).
- **Owner verification (independent, Python 3.11).** `make bt2a-rehearsal` exit 0 — all frozen-reference known answers reproduce, 5/5 drills caught (catch rate 1.0) incl. the bridge-layer `tampered_instance`, release blocked as expected, hypothesis ratio 1.0; the rehearsal report is idempotent (re-run → no tree diff). `make gate` exit 0 (57 gates incl. `bt2_bar` + `runbook_staleness`); new golden `tests/golden/test_golden_bt2a.py` (10 cases: gate green on committed reference, skip-when-absent, RED on below-threshold catch / ratio violation / release-not-blocked / reference-not-reproduced / malformed abort clause, real release-perimeter blocks, hypothesis ratio = 1, missed-injection RED); `make reproduce-analysis` byte-identical + content-equivalent; registry 12/12 failing; no pyyaml/pandera/dependency change; version-safe stdlib; `package_manifest` regenerated LAST. CI runs `make bt2a-rehearsal` in a clean environment and asserts the report is byte-stable. **After BT2-A merges, the M0 → M5 → BT2-A build is COMPLETE.**

---

## M5 — Framework/pack separation + self-measurement (BATCH A MERGED #11 `4c739f0` · BATCH B MERGED #12 `1da334b`)

### M5 Batch B — seeded-defect drill harness + held-out eval tier + eval CI + operator runbook (plan §9.3, §10 M5 row)

- **What was built (Codex workhorse, owner-reviewed).** `scripts/seeded_drill.py`: a rotation of one drill per §9.3 defect class, each injecting a known-fabricated defect into a rehearsal fixture and asserting the correct gate/judge/integrity check REALLY blocks it (`fabricated_run_manifest`→Judge/swarm_run_manifest, `fabricated_citation`→citation_integrity, `fabricated_claim_number`→claim_evidence_ledger, `false_lemma`→theoretical_falsification, `tampered_instance`→instance_manifest_conformance) — `caught` is derived from the actual gate result (`not result.ok` / a specific failure reason / a nonzero Judge exit), NOT a self-asserted boolean; a synthetic MISSED injection raises `MissedInjection` (the harness cannot pass it). A held-out eval tier (`tests/held_out/cases.py`, 5 fresh cases OUTSIDE the default `test*.py` pattern — `make eval-heldout`, not in `make test`) is the Goodhart control. Eval CI path-detects `contracts/`/`docs/prompts/`/gate-surface changes and fails the golden suite on regression. `docs/operator_runbook.md` covers the supervise lifecycle + 5 escalation-class playbooks + 4 L3 attestations (with bundle-bypassing checks) + drill commands + all 56 gates; a `runbook_staleness` gate cross-checks it against `_ALL_GATE_NAMES` + `ESCALATION_CLASSES` and fails on an uncovered entry. task_lint now requires `program_id`/`program_node` on schema-v2 W6/W7/W8 tasks (closing the M4-B dev-time gap). The §4.3 OS-scratch sandbox is recorded HONESTLY: `os_enforced: false`, `effective_network: proxy_environment_only` — no confinement claim on a host without an enforceable network namespace.
- **Owner verification (independent, Python 3.11).** `seeded_drill.py --all` → 5/5 caught, catch_rate 1.0; the drill catch logic derives `caught` from real gate blocks (confirmed by reading each drill). `runbook_staleness` green (56 gates, 5 escalation classes). Full suite **533 OK** (skipped=1); the drill goldens use an ISOLATED temp `journal_root` (no live-journal pollution); `make gate`/`make reproduce-analysis` green; registry all-failing; no pyyaml/pandera/dependency change; version-safe stdlib.
- **Owner-found defect → M5-B fix round (dual-vendor pending).** The operator CLI `seeded_drill.py --all` defaults `journal_root=Path(".")`, so it writes its `seeded_drill` catch-rate events to the LIVE research journal `reports/status/events/events.jsonl` — which the compliance disclosure reads (event-journal `absent`→`present` + file hash), breaking `venue_compliance`/`reproduce-analysis` idempotency and polluting the STR manuscript's compliance provenance with test-drill events. The committed state is clean (the ephemeral journal is runtime state, removed; disclosure back to `absent_not_fabricated`, reproducible), but the CLI must be redirected to a drill-specific journal (and the disclosure must never count drill events) — deferred to the fix round after the dual-vendor pass independently assesses the drill-journal design.

### M5 Batch B — fix round (owner-implemented inline; dual-vendor adversarial review of the frozen `bf21f15` diff — LANDED)

- **Reviews.** Codex critique: 2 blockers + 2 majors (verdict NOT-MERGEABLE), and CONFIRMED the drill harness genuine (all 5 `caught` trace to real gate/judge blocks; the missed-injection RED path exists; held-out statically excluded from `make test`; no dependency change). Claude adversary: ship-with-fixes — independently confirmed the harness soundness and widened the owner-found drill-pollution blast radius (the polluting command is reached by two DOCUMENTED operator surfaces, `Makefile:drill` and the runbook, so the fix must change the DEFAULT, not merely add a flag). This whole fix round was owned INLINE (not delegated): the tripwire flags it end-to-end (confinement claims, eval-integrity Goodhart control, protocol-invariant integrity gates — the "swarm measures itself" surface), and every fix has a crisp testable oracle.
- **Recorded reviewer DISAGREEMENT on the F2 blocker (not averaged; adjudicated on the code by the owner, per the tripwire's science-integrity ownership).** Codex: the effective-confinement report is a prohibited FALSE-confinement claim (`credential_isolation: environment_scrub_only` + `effective_network: proxy_environment_only` are untrue). Claude: "OS-sandbox honesty holds." **Owner adjudication (traced the sources): Codex is right, Claude's verdict was an oversight.** `_offline_subprocess_env()` scrubbed NO credentials (only injected a dead proxy), and the LIVE auditor subprocess was launched with no `env=` at all (inherits the ordinary environment) — so both labels were false for the live backend and the credential label false everywhere. Claude verified only that `os_enforced: false` is honest (true) without tracing the subprocess launches. **On Codex's secondary framing that M5-B "weakened the prior gate from `network: off` to unchecked `requested_off`": rejected.** The old `network: off` was ITSELF the false claim (no OS enforcement here); `requested_off` is the honest direction and M5-B simultaneously ADDED the confinement checks — reverting to `off` would reintroduce a lie. Fix keeps `requested_off` and makes the labels TRUE.
- **Fixes (all inline, all with regression tests).**
  - **F2 (confinement honesty).** `_offline_subprocess_env()` now actually strips credential-named variables (denylist of `*_TOKEN/*_SECRET/*_KEY/AWS_*/ANTHROPIC*/...`) so `environment_scrub_only` is TRUE; the live auditor subprocess gets `_live_backend_env()` (scrubs every credential EXCEPT the vendor's, which it needs to reach its API); `effective_confinement(backend)` is backend-aware — `mock` (CI default, no auditor egress) reports `environment_scrub_only`/`proxy_environment_only`; `live` honestly reports `vendor_credential_retained`/`vendor_api_transport_only`, never a scrub/proxy it does not apply. Schema enums + gate widened to the honest labels; gate now also validates `credential_isolation`.
  - **Drill-journal pollution.** Drills write to an ephemeral, gitignored `reports/status/drills/drill_events.jsonl` (`swarm_events.DRILL_JOURNAL_PATH`), NEVER the compliance journal; belt-and-suspenders, `generate_disclosure.py` excludes any `seeded_drill*`/drill-actor event. `make drill` now leaves the tree clean and the disclosure untouched.
  - **F1 (held-out genuinely distinct).** The 5 mirror cases (which shared golden fixtures/gate/reason and, for the referee, PRESCRIBED the verdict via `write_mock`) are replaced with 5 independently-constructed cases (no import of the golden test modules) asserting reasons exercised by NEITHER the golden suite NOR the drill rotation: `comparative_static_violated` ×2 (finite-difference + explicit-derivative branches), `content_binding_target_missing`, `instance_manifest_schema_violation`, `citation_snapshot_key_mismatch`. The un-producible-offline live-referee held-out case is honestly dropped (flagged tier-c/live-calibration), not faked. READMEs corrected to the verified distinctness contract.
  - **F3 (escalation classification).** `swarm_events.escalate()` now rejects an `escalation_class` outside the registered `ESCALATION_CLASSES`; the genuine human-escalation emitters are tagged (`plan_awaiting_human_approval`/`human_question`/`referee_owner_waiver`→`blocked_with_human`, `hypothesis_retirement_escalated`→`hypothesis_task_retirement`). Two-tier taxonomy documented in the runbook (operational `escalation=True` vs registered human-escalation class): a new human-escalation class cannot ship without registration + a runbook playbook (`runbook_staleness` then enforces it).
  - **F4 (freeze historical task exemptions).** The immutable exemption ledger is now hash-pinned by a pack-declared digest (`contracts/pack.json` `integrity.historical_exemptions_sha256`; kernel stays project-agnostic — it reads the pack's declaration): a silently self-added v1 W6/W7/W8 task exemption (the mandatory-tagging bypass) changes the ledger bytes → `historical_exemptions_ledger_mutated` → `make gate` RED. The `tasks` section is added to the per-entry file-hash sweep. Only the two grandfathered historical W6/W7 tasks (T060, T070) remain exempt; any new exemption is a deliberate, reviewable, dual-file change. Test-fixture helper `pin_pack_to_ledger` keeps fixtures honest.
  - **Minors.** `run_rotation` scores a red run over the FULL intended rotation (unrun == uncaught — a conservative lower bound, never overstates on a miss); the eval-CI change filter broadened to every gate-behaviour-bearing script (`swarm_events`, `integrity_audit`, `generate_disclosure`, `pack_config`, `falsify_claims`, `replication_package`).
- **Owner verification (independent, Python 3.11; clean-checkout semantics).** `make gate` exit 0 (56 gates incl. `runbook_staleness`, `historical_exemptions`, `integrity_audit`); `make eval-heldout` 5/5; `seeded_drill.py --all` 5/5 caught with ZERO tree pollution (drills gitignored, no compliance-journal write); `make reproduce-analysis` byte-identical + content-equivalent after the manifest was regenerated LAST (this exec-log + runbook + scripts are packaged members); registry 12/12 failing; no pyyaml/pandera/dependency change; version-safe stdlib.

- **Second dual-vendor pass on the fix diff `bf21f15..22f9b37` → second fix round (owner-implemented inline; LANDED).** Both vendors reviewed the FROZEN fix commit (self-assessment redacted). They CONVERGED (unlike the first-round F2 disagreement) on 6 confirmed issues; both confirmed F4, the drill-journal split, and the red-run scoring SOUND. Codex rated the credential-scrub leak a blocker, Claude a minor-with-mitigation (the leaked var is neither exfiltratable nor tool-readable behind the dead proxy + command/tool allowlist) — a severity disagreement on a defect both confirmed; owner decided to fix regardless (an allowlist is strictly safer). Fixes:
  - **F2 (deeper).** Credential scrubbing switched from a leaky name DENYLIST to a benign-only ALLOWLIST (`DATABASE_URL`/`REDIS_URL`/bare `*_KEY`/`*_PASS`/`CI_JOB_JWT` no longer leak — secrets are excluded by construction); the scrubbed+dead-proxied env is now applied to EVERY audit subprocess including the `_git`/worktree helpers (a `post-checkout` hook can no longer inherit credentials/network); the live-backend network label corrected from the overstated `vendor_api_transport_only` to the honest `unrestricted_process_egress` (no process-level destination control is enforced); the `integrity_audit` gate now BINDS the confinement tuple to the report's backend and pins `os_enforced:false`, so a hash-bound but dishonest report (live backend claiming proxy-only/full-scrub, or any report claiming OS enforcement) fails.
  - **F1 (deeper).** Dropped the `test_m3a_modeling_battery` import (a `make test` module — the earlier claim was overstated) by inlining the theoretical-claim builder, and replaced the one held-out case whose reason (`content_binding_target_missing`) IS asserted by `make test` (`test_m3a:563`) with `citation_snapshot_directory_after_as_of` (future-dated evidence). All 5 held-out reasons re-verified by grep as absent from `tests/**/test_*.py` AND the drill rotation.
  - **F3 (deeper).** `budget_exceeded` now carries `escalation_class="budget_breach"` (a real human escalation that was riding untagged). Added CI TEETH independent of the best-effort journal swallow: `test_no_emitter_uses_an_unregistered_escalation_class` statically asserts every emitted `escalation_class` literal ⊆ `ESCALATION_CLASSES`. Runbook now documents which classes are auto-emitted (`blocked_with_human`/`hypothesis_task_retirement`/`budget_breach`) vs owner-raised (`judge_disagreement`/`unsatisfiable_constraints` — the v1 runtime doesn't auto-detect these; M1+ supervisor work).
  - **Disclosure.** `generate_disclosure` now FAILS CLOSED if a `seeded_drill` event is ever found in the compliance journal (defense-in-depth: drill bytes can never be hash-bound as released provenance).
  - **Minor.** Red-run drill summary reports `injected` = actually-attempted and `planned` = full rotation separately (catch rate still scored conservatively over `planned`).
  - **Verified (Python 3.11, clean-checkout):** full suite **537 OK** (skipped=1); `make gate`/`make eval-heldout` (5/5)/`seeded_drill --all` (5/5, zero pollution)/`make reproduce-analysis` (byte-identical + content-equivalent) green; registry 12/12 failing; no dependency change; package_manifest regenerated LAST.

- **Third focused pass on the second-round delta `22f9b37..33d48e9` (Codex; owner-fixed inline; LANDED).** A single targeted verification of the second round found 2 confirmed majors in the newly-added code, both fixed: (a) the `integrity_audit` gate's backend binding gave the LIVE confinement tuple to any non-`"mock"` backend, so a consistent but unimplemented/typo'd backend (`claude_typo`) passed — now dispatched through an explicit `{mock, claude}`→tuple map that REJECTS unknown backends (`integrity_audit_unknown_backend`; regression test added); (b) the F3 static-scan test's regex matched only the dict-literal emitter form, missing the `event["escalation_class"] = "budget_breach"` SUBSCRIPT-ASSIGNMENT form it was added to protect — regex broadened to cover both forms (a typo in either is now caught by `make test`). Both held-out comparative-static cases, the future-dated-citation reason's absence from `make test`, the disclosure fail-closed, and the red/green drill scoring were re-confirmed SOUND. Verified: full suite **538 OK** (skipped=1); `make gate`/`make reproduce-analysis` green; registry 12/12 failing; no dependency change; package_manifest regenerated LAST. This converged the review (the third pass's findings were confined to the second round's own new lines).

Scope: plan §4.5 step 1 / M5a — config extraction (project literals → pack config), executable schemas, framework-test decoupling, `swarm init` scaffold, STR-as-first-pack (zero operational-kernel edits), versioned kernel-pack interface + compat preflight (Batch A). Eval CI held-out tier + seeded-defect drill harness + operator runbook = Batch B; physical packaging (M5b) is a deferred decision point.

- **M5 Batch A (Codex workhorse, owner-reviewed; boundary corrected in fix round).** Project literals consumed by the reusable operational kernel under `scripts/` moved into `contracts/pack.json` (validated by `contracts/schemas/pack_config_v1.json`): paper artifact basename/build dir, panel/table/figure/exhibit names, protocol headings, panel-schema paths, workstream semantics, registry path, package identity, historical-manifest names, scientific panel-column names, regime-series names/defaults, and the literature workstream label. `scripts/pack_config.py` reads them; `gate_framework_contract` validates pack-config shape rather than STR literals. The `src/` tree is not kernel: it is the first pack's intentionally STR-specific analysis/ETL/validation science body and a new pack replaces it. Executable schemas are JSON via `json.loads` (NO pyyaml/pandera); ETL and validation derive dataframe field order/nullability from pack-owned schemas. `scripts/swarm_init.py --mode empirical|modeling|hybrid` creates an inactive contracts/orchestration scaffold plus a documented `src/` placeholder, not a runnable science pipeline. `contracts/kernel_interface.json` describes kernel version 1.1.0 (minor bump for the additive namespace-policy field), schema/gate/executor versions, breaking policy, and the documentation-only future reservation `refs/swarm/claims/<program>/T###`; the v1 runtime remains single-program.
- **Fix-round verification target (Python 3.11).** ZERO STR project literals remain in the operational kernel `scripts/`; STR literals remain legitimately in pack-owned `src/`. `scaffold: true` is honored only while the repo has no claims, computed paper values, processed outputs, run manifests, non-scaffold registry entries, or built report outputs; an instantiated repo fails closed. `pack_compat` reads the executing kernel's descriptor, never the pack copy. A generated scaffold passes its staged gate form and mock orchestration test, but its science pipeline remains intentionally unimplemented. Zero pyyaml/pandera; no dependency/lockfile change (pyproject `name` → `research-swarm-kernel` is the only pyproject edit; deps/version unchanged).
- **Dual-vendor review + owner verification (Python 3.11).** Codex critique + Claude adversary reviewed the frozen M5-A build. Claude confirmed the operational-kernel extraction genuine and supplied the kernel-vs-pack boundary framing (`scripts/`=kernel, `src/`=pack science) adopted above; both flagged the overstated "zero literals in src/" claim (corrected). Two BLOCKERs (Codex-confirmed, both fixed + owner-re-verified directly): (A) `scaffold: true` was an UNBOUND gate bypass a live pack could set — now fail-closed (`scaffold_asserted_on_instantiated_repo`; owner confirmed the live STR pack with `scaffold:true` fails `make gate` with the instantiation evidence listed); (B) `pack_compat` read the pack's COPIED descriptor — now sources `_EXECUTING_KERNEL_ROOT = Path(__file__).resolve().parents[1]`. Majors fixed: the kernel gate column/metric names extracted to `contracts/pack.json` (a non-STR `day/unit/gross_value/cost_value/cost_share` pack passes the kernel analysis gates), the pack-config schema now declares every unconditional consumer key per mode, the venue-profile test now invokes the real `check_manuscript_section_profile` with a non-STR profile + a negative mutation, the pyproject/pack-identity two-sources-of-truth collapsed (generic kernel name), and the descriptor's program-namespaced refs marked reservation-only. Owner-verified: full suite **527 OK** (skipped=1); `make gate` exit 0; `make reproduce-analysis` byte-identical; registry 12/12 failing.

---

## M4 — Computed paper, programs, replication (BATCH A MERGED #8 `6d3ddc3` · BATCH B MERGED #9 `870ed48` · BATCH C MERGED #10 `8738b8d`) — COMPLETE

Scope: plan §7.1 computed-paper machinery + F14 perimeter (Batch A, merged); §7.2 mode-parameterized W6/W7 program templates + exhibits manifest + failing-first paper registry (Batch B, merged); §7.3/§7.4 replication package generator + audit + venue/authorship/disclosure + release modes (Batch C).

### M4 Batch C — replication package + audit + venue/authorship/disclosure + release modes (plan §7.3/§7.4)

- **What was built (Codex workhorse, owner-reviewed).** `scripts/replication_package.py` generates an AEA-shaped package from repo state (processed data + code + master script + exhibits/tables/paper_values + a README RENDERED from artifacts: declared deps from `pyproject.toml`, exact runtime versions from processed-manifest `environment.dependencies` blocks — truthfully "unlogged" where absent — raw-availability from raw manifests + the existing loss amendment, exhibit mapping from the exhibits manifest/catalog). `replication_package_audit` gate (mode-aware, 3 profiles): empirical Functional live / **Reproduced pending a non-author clean-room** (NOT fabricated); modeling + hybrid Functional+Reproduced on `tests/fixtures/m4c_{modeling,hybrid}/` toy projects. The fixed hybrid audit copies the package to a clean workspace, removes the committed instance/convergence/experiment outputs, runs a generator that writes `instance_manifest.json`, regenerates the experiment output, compares every declared output to its byte/content baseline, and verifies the processed→regenerated-instance→regenerated-output hash chain end-to-end; a hash-printing stub or a master that merely consumes committed instances fails. `raw_retention` requires a verified `file://` archive, a hash-bound offline receipt for remote storage, or the `raw_evidence_unavailable` amendment; a URL is not evidence by syntax alone, and the 6 committed absent snapshots remain amendment-satisfied (no invented URL). `contracts/venue.yaml` + `contracts/authorship.yaml` + `scripts/generate_disclosure.py` → `reports/paper/disclosure.md` DERIVED from run/review manifests + provenance annotations + the event journal (regenerable, byte-checked). Two release modes (mainstream/ai_native); `venue_compliance` gate is a NON-bypassable release-perimeter block that refuses a submission on null author / unresolved consent or venue-policy evidence / missing required statements / consent-incompatible venue / mode conflict / missing manuscript perimeter or instantiated program / non-regenerable disclosure — closing the M4-A/M4-B deferred "declared submission" signal. Also added `contracts/manuscript_sections.yaml` (the pinned canonical section set, closing an M4-B residual).
- **Compliance correction (owner-verified, external-facing).** The reference manuscript front matter named the swarm as author (`author: "autonomous-agentic-research-swarm"`) — a real ICMJE/COPE violation surviving M3a–M4B. M4-C corrects it to `author: null` (rendered from `authorship.yaml`, which carries `human_author_of_record: null`, `consent: not_obtained`, marked reference-implementation). The swarm is documented ONLY in the disclosure. Owner-confirmed the `index.qmd` diff is author-field-ONLY (no science/prose change); `paper_values.json`/tables/sidecars byte-unchanged.
- **Owner verification (independent, Python 3.11).** venue.yaml/authorship.yaml are truthful REFERENCE values (`status: reference_implementation`, `real_submission: false`, `ai_as_author: prohibited`, null author) — no fabricated venue acceptance, human author, archive URL, or passing status. `make gate` exit 0 / 0 `ok=False`; `replication_package_audit` empirical Reproduced=False (honest), modeling/hybrid pass on fixtures; `venue_compliance` reference_bundle_valid + submission_eligible=False; `raw_retention` amendment-satisfied ×6; registry truthfully 12/12 failing. `make reproduce-analysis` byte-identical (disclosure + README + package_manifest added to the byte set). Full suite **504 OK** (skipped=1) under Python 3.11 (re-run by owner — the delegate's full run was under 3.13); 9 M4-C goldens; no dependency/lockfile change (uv.lock does not exist and was not created; versions sourced from pyproject + logged runtimes).
- **Genuine ambiguities the delegate correctly returned (not fabricated).** Codex STOPPED on first pass rather than guess: (1) `uv.lock` does not exist → owner resolved to source versions from `pyproject.toml` + processed-manifest runtime logs; (2) no consented human author → owner resolved to a reference-only author-less contract that BLOCKS submission. Ideal anti-fabrication delegate behavior.
- **M4-C fix-round residuals (explicit, not waived).** The audit now corroborates master execution and regenerated output bytes, but the `non_author_agent` identity remains an out-of-band attestation rather than something deterministic code can prove; M5 owns that identity/attestation layer. Human-consent authenticity is likewise an owner-attestation fact: the deterministic gate proves only that the claimed consent and venue-policy evidence artifacts resolve and hash-match. Live remote archive resolution and cold-storage upload also remain out-of-band Operator/M5 work: the offline gate verifies a committed hash-bound receipt or the truthful loss amendment, never a remote URL by syntax alone.

### M4 Batch B — programs, exhibits manifest, failing-first registry (plan §7.2)

- **What was built (Codex workhorse, owner-reviewed).** Three mode-parameterized program templates under `contracts/program_templates/` (validated by `contracts/schemas/program_template_v1.yaml`): empirical W6 `estimation_plan→estimation→robustness→exhibits` + W7 `claims_map→outline_lock→section_draft→integration→referee_panel→revision_cycle→compliance_package`; modeling theory `formalization→proposition_registration→proof→derivation_check→results` + experiment `experiment_plan→runs→sweep→exhibits`; hybrid bridge `calibration→instance_generation→grid_execution→counterfactual→exhibits`. The Planner prompt now names and embeds the mode's canonical template (instantiate, never improvise) and routes `revision_cycle` to `generate_revision_tasks.py`. New gates: `program_conformance` (parses the task DAG: required-node presence, `depends_on`→`dependencies` ordering, mode-foreign-node + task_kind/role/workstream detection — staged/inactive until the program is instantiated on a lock), `exhibits_manifest` (the analysis→paper interface `reports/exhibits/manifest.json`: every manuscript-referenced figure/table is a hash-bound entry with builder + 8 hash-bound inputs + figure self-QA facts derived by introspecting the real Matplotlib axes — labels/legend/units/alt; modeling numeric outputs must key into `paper_values.json`), and `paper_registry` (`reports/paper/registry.json`: 9 required sections + 3 exhibits, all seeded truthfully `failing` because no journal-bound referee reports exist yet).
- **Owner verification (independent, Python 3.11).** `make gate` exit 0 / 0 `ok=False`; the 3 gates wired into `_MODE_INDEPENDENT_SCIENCE_GATES` + `_ALL_GATE_NAMES`. **Registry authority is real, not self-assertion:** a `passing` entry requires the gate to independently re-hash the canonical ID-bound artifact AND find a journal-bound (`_referee_report_journaled`), schema-valid, `overall=supported` M3b referee report — a hand-edited `passing` fails. The registry is truthfully all-`failing` (no fabricated passing). **The release-perimeter clause is reachable and blocks:** `paper_registry` and `program_conformance` run in release-perimeter mode and are unconditional release blocks, including under `--allow-gate-failures`; this makes "a figure-only slice is not a release" mechanical at the release perimeter. Development-time program tagging remains Planner-cooperative pending the M5 task-lint requirement below. `make reproduce-analysis` keeps the manifest byte-identical (added to the byte set). No dependency/lockfile change.
- **M4-B residuals (explicitly deferred, not waived).** Program conformance is mechanical at the release perimeter; development-time tagging is Planner-cooperative pending an M5 planner-contract enhancement that makes `program_id` mandatory in task lint for template-controlled W6/W7/W8 tasks. M4-C/BT2 must bind reviewed artifact-version SHAs to registry SHAs and journal the referee-report byte hash, because current reports name artifact paths and the journal binds the referee run rather than report bytes. M4-C also owns a pinned canonical section set plus per-section referee granularity (live `##` headings currently define the section set and one manuscript-level supported report can authorize multiple section entries). Modeling-profile numeric keying remains defense-in-depth: SVG numeric axes and per-value CSV granularity are deferred until the modeling reference project is built out. Figures nested in included Quarto sub-documents are likewise not recursively scanned until M4-C.

Batch record (Batch A):

- **M4 Batch A.** The STR analysis path emits a typed, source-selected `paper_values.json` from the same in-memory regime table used for the committed table, plus deterministic `.data.json` content sidecars for both non-byte-stable SVG figures. Manuscript reportable numerics resolve through value keys; the computed-paper gate re-reads and hash-checks each source artifact, recomputes each selected value to display precision, rejects bare prose numerics and unresolved keys, and enforces claims-ledger agreement. Release assembly requires the manuscript sources and both committed processed panels as hash-recorded perimeter members and fails closed on absence.
- **Owner review + F14 over-reach fix (inline; release-config + science-gate surface — tripwire).** Owner independently re-verified the science invariance under Python 3.11: `make reproduce-analysis` recomputes tables + `paper_values.json` + both sidecars byte-identically from the committed processed panels; the `make paper` resolver reproduces the pre-change manuscript **byte-identically** (`git show main:reports/paper/index.qmd | cmp - build/index.resolved.qmd` → exit 0), so no scientific value changed; the computed-paper gate is genuinely a recompute-from-source (hash-binds each source artifact, extracts at `source_selector`, requires source==value==display), not a self-comparison. **Regression the delegate's self-reported "445 tests exit 0" missed:** the full suite actually failed 5 tests (2 `test_release_catalog_integrity`, 1 `test_m3b_referee`, 2 `test_m3a_modeling_battery`), all one root cause — F14 perimeter enforcement was **unconditional**, so every release/gate path over a manuscript-free repo (modeling-only profile, empty hybrid fixture, scratch catalog/referee repos) failed demanding an empirical manuscript that legitimately does not exist. Fixed by gating **all four** F14 enforcement sites on manuscript presence (`reports/paper/index.qmd` on disk) with one uniform rule: `assemble_release_manifest` perimeter block, its render-manifest-inputs check, `validate_release_manifest` (post-write) perimeter check, and `gate_manuscript_computed_paper` are active **iff a manuscript ships** — and when a manuscript IS present, a missing declared member still fails closed (F14 intent preserved; the gate additionally fails closed if *either* manuscript surface exists without the other). Both directions are locked by tests: the 5 recovered tests guard "no manuscript → not enforced"; Codex's `test_release_assembly`/`test_m4_computed_paper`/`test_golden_m4` guard "manuscript present → fail closed". Verified: `make gate` exit 0 with `manuscript_computed_paper` still `status=active`/16 values on the real repo; full suite 445 OK (skipped=1); no dependency or lockfile touched. Per-profile panel membership for the modeling/hybrid replication profiles is refined in M4 Batch C.
- **Adversarial review + fix round (Claude adversary on the frozen `main..69206c2` diff; Codex critique lane rate-limited — cross-vendor pass rerun on the re-frozen diff after reset).** The Claude adversary independently confirmed the science is sound and value-preserving — recompute is a genuine recompute-from-source (hash-bind + selector extraction + three-way compare, not theater), all 16 values re-derive from their committed source artifacts, every `{{value:KEY}}` display is byte-identical to the literal it replaced, `make reproduce-analysis`'s byte check truly regenerates, no dependency/lockfile touched, no prior gate weakened. Two MAJORs (both against the owner's own F14 conditional-gating fix) and four minors were raised; disposition:
  - **F-M4A-1 (MAJOR, fixed) — the four F14 sites disagreed on activation.** The gate used "either surface exists", the perimeter used `index.qmd` only, so a partial-deletion state (`paper_values.json` present, `index.qmd` gone) was gate-active-but-perimeter-suppressed. Fixed by routing all four sites (`assemble_release_manifest` block, its render-manifest-inputs check, the post-write `validate_release_manifest` check, and `gate_manuscript_computed_paper`) through one shared `_manuscript_surface_present()` helper = (`index.qmd` OR `paper_values.json`) present. Partial deletion now fails BOTH consistently; both-absent skips both. Locked by `test_partial_manuscript_deletion_keeps_gate_active_and_fails` + `test_no_manuscript_surface_skips_gate`.
  - **F-M4A-4 (MINOR, fixed) — rounding-mode mismatch.** The gate recomputed with Decimal `ROUND_HALF_UP` while the generator produces every value with Python `round(float(source), precision)` (banker's, on floats); a future source value on a rounding boundary would make the gate reject the repo's own output (fail-closed, but a spurious CI break). Fixed: the gate now recomputes with the identical `round(float(source), precision)`; still fail-closed against genuine divergence (any real forgery differs at display precision, ~1e8× above float round-off). Locked by `test_recompute_matches_generator_rounding_on_boundary` (2.675 → generator 2.67 accepted; HALF_UP artifact 2.68 rejected).
  - **F-M4A-2 (MAJOR → bounded residual, owned by M4-C + BT2).** An empirical project that deletes/never-builds its ENTIRE manuscript (both surfaces absent) reads as no-manuscript and ships green — the continuous gate + `render_qa` + `claim_evidence_ledger` all skip. This canNOT be closed at continuous-gate level without breaking the legitimate "empty project is green" invariant (the scaffold fixtures set `features.paper: true` with no manuscript by design, so project-mode is not a usable activation signal here). Correct closure is a release-stage "declared paper submission" signal — M4-C's venue/authorship/release-mode contract (a `mainstream`/`ai_native` release declares a paper, making the manuscript perimeter mandatory) — plus a BT2 seeded drill ("delete the manuscript → the release must catch it"). The real STR repo is fully enforced today (manuscript present); the F-M4A-1 unification already closes the partial-deletion case.
  - **F-M4A-3/5/6 (MINOR → documented boundaries).** (3) The bare-numeric scanner and `claim_evidence_ledger` share structural exemptions (fenced code, `{{< include >}}` targets, frontmatter, `fig-alt`) — a reportable number placed there evades both; no live instance exists (the one `fig-alt` "14-day" is benign; the single `{{< include >}}` target is byte-pinned via `reproduce_analysis`). Defense-in-depth on these vectors is inherently limited; semantic placement is the M3b referee + §6.5 integrity-audit's job. (5) The claim ledger enforces claim→paper_value completeness but not the reverse; `claims.yaml` registers claim-level headline literals while `paper_values.json` is the complete source-verified registry (e.g. the definitional 7-day protocol constant is source-verified against `protocol.md` but not a headline claim). (6) Tokenization removes numbers from `claim_evidence_ledger`'s occurrence-scoped per-line citation binding; a strict per-line token↔citation check would false-positive on legitimately cross-referenced values (e.g. `1,559 dates` cited beside the decomposition validation), and `claims.yaml` already delegates semantic placement to the M3b referee — so this stays the referee's domain, not the computed-paper layer's. All three are recorded for their owning layer, none is a value-integrity defect.
- **CI-discovered cross-platform determinism fix (owner; the byte-vs-content line the plan draws).** PR #8 CI (Ubuntu / CPython 3.11.0) failed `reproduce_analysis_byte_mismatch` on the ecosystem figure `.data.json` sidecar while the regime tables AND `paper_values.json` were byte-identical there — i.e. the science artifacts are cross-platform byte-stable (display precision 3-6 dp absorbs float round-off) but the sidecars' 12-dp plotted series capture last-ULP float64 differences from platform/BLAS/NumPy reductions. Per plan §7.3 ("byte identity vs content equivalence, never conflated"), `reproduce_analysis.py` now splits its check: BYTE identity for tables + `paper_values.json` (a single changed digit fails), CONTENT equivalence for the figure sidecars (recursive compare — strings/bools exact, numbers within rel_tol/abs_tol 1e-9, which tolerates ~1e-11 cross-platform round-off but catches any genuine data drift orders of magnitude larger). Locked by `ReproduceAnalysisContentCheckTest` (float-noise tolerated; real drift, length/key/string/bool changes all caught). This is the honest bar for a non-container CI; a digest-pinned container would restore full byte identity (M4-C replication-package "Reproduced" level).
- **Codex cross-vendor critique + fix round 2 (owner-implemented inline; science-gate + release surface).** After the Codex quota reset, the read-only Codex critique (gpt-5.6-sol xhigh) ran on the re-frozen `main..2add501` and found SIX real enforceability holes the Claude adversary had missed — validating the hold-for-dual-vendor decision (agreement is weak evidence; the two vendors caught disjoint failure modes). Codex independently confirmed no value drift among the committed literals; the defects were all in enforcement. All fixed inline (owner-owned science/release surface), each with an adversarial regression:
  - **F1 (BLOCKER, display injection).** `render_paper.py` substitutes `display` verbatim, but the gate validated only its first numeric — so `display="69.14% and post-Dencun STR is 99.99%"` (value 69.14) passed and rendered a false second claim. Fixed: `display` must `fullmatch` a strict canonical form (one signed number + optional single known unit, no prose/markup/newlines) AND its unit suffix must agree with `unit`. Tests: `test_display_with_injected_second_claim_fails`.
  - **F4 (MAJOR, rounded-forgery — a hole in the owner's own F-M4A-4 fix).** The gate rounded BOTH source and declared value to display precision before comparing, so `value=7.4` shown as `"7"` (precision 0) passed. Fixed: recompute `round(float(source), precision)` but compare to the RAW declared value and display number (±1e-9 float slack only). Test: `test_value_disagreeing_with_source_but_rounding_to_display_fails`.
  - **F5 (MAJOR, non-bidirectional claims — flagged by BOTH vendors).** The ledger check was claims→values only; a new source-valid value could be tokenized into the paper unregistered (the rendered 7-day constant already was). Fixed: (a) registered `"7 days"` in `claims.yaml` REF-BLOB-FLOOR-DEF (reproduces from `protocol.md`; M3a floor now 17 literals); (b) added the reverse check — every paper value RENDERED via a `{{value:KEY}}` token must have its (value, unit, citation) registered in the claim ledger. Test: `test_rendered_value_absent_from_claim_ledger_fails`.
  - **F6 (MAJOR, sidecar tolerance too loose — a hole in the owner's own byte-vs-content fix).** `rel_tol=abs_tol=1e-9` was ~100x the real post-12dp noise (~1e-12..1e-11), hiding drift (a near-zero blob-fee flip to 0, or ~1e-7 on a ~950 series). Tightened to `1e-10` and added explicit non-finite (NaN/inf) rejection. Tests: `test_drift_above_tightened_tolerance_is_caught`, `test_non_finite_values_are_rejected`.
  - **F2 (BLOCKER, rendered-without-sources).** Total manuscript deletion could ship STALE rendered HTML/PDF while F14 perimeter suppression hid the missing sources. Fixed: `assemble_release_manifest` fails closed (`release_rendered_paper_without_sources`) when the canonical build outputs exist (`paper_status=present`) but no manuscript source surface does. The `test_m3b_referee` release fixture was materializing exactly this invalid state (renders without sources) and is now a complete, coherent surface (sources + panels + a render_manifest declaring all six perimeter inputs). Test: `test_rendered_paper_without_sources_fails_closed`. The narrower not-yet-written-manuscript activation residual (no renders, no sources) remains owned by the M4-C release-stage/venue contract + a BT2 drill.
  - **F3 (MAJOR, rendered channels).** The bare-numeric scanner skipped whole lines containing an `{{< include >}}` and never constrained include targets. Fixed: strip only the shortcode (scan the rest of the line) and require every include target to be a reproduce-analysis-verified artifact (`manuscript_unverified_include_target`). Tests: `test_unverified_include_target_fails`, `test_bare_numeric_sharing_a_line_with_include_is_scanned`. Intentional, now-documented exemptions (answering Codex's open question): `fig-alt` accessibility text and numbered headings are structural not prose claims; the one included table (`str_regime_summary.md`) is byte-reproduced by `reproduce-analysis`, so its cells are integrity-protected as the computed artifact itself rather than restated prose; spelled-out numerals remain out of scope for a digit scanner (the M3b referee reads the full rendered paper).
- **Scope deferred (not built in Batch A).** Plan §7.2 W6/W7 mode-parameterized program templates and exhibits manifest remain M4 Batch B. Plan §7.3 replication package generator, three-profile `replication_package_audit`, cross-layer hash chain, clean-room exercise, and §7.4 venue/authorship/disclosure/consent contracts plus raw-snapshot retention/amendment remain M4 Batch C. Full Quarto→LaTeX/PDF fidelity stays on-demand because CI enforces the pure-Python resolution core. Live/raw→processed recomputation is not part of this regression reference because the raw evidence layer no longer exists; Batch A starts from the committed processed panels.

---

## M3c — Inference module (branch: `milestone/m3c-inference`) — FIX ROUND IN PROGRESS

Scope: plan §5.2 `statistical_reporting`, §6.4 inference toolkit, and the M3c acceptance row.

Fix-round record:

- **M3c deterministic-disposition fix round.** Corrected chronological Driscoll–Kraay score aggregation and fail-closed cluster identifiers; projected non-PSD two-way clustered covariance estimates onto the PSD cone; changed the piecewise-constant break routine from always choosing `max_breaks` to BIC selection over every feasible break count; made the statistical-reporting gate validate finite estimate/SE-or-CI/N semantics, ledger↔table headline parity, all causal claims, recursive registered table surfaces, content-bound spec-curve inputs, and a recomputed spec-curve verdict. The locked clustered-VCE plus `date_aggregate` grid combination is rejected during config validation rather than during estimation.
- **D-M3c-1 (output recomputation → M4).** M3c content-binds the processed-panel input, locked parameters, and recomputed spec-curve verdict. Independent recomputation of regime `break_dates`/`ssr` and every table `estimate` from the hash-bound panel plus active lock remains the §6.5 integrity-audit tier exercised on the M4 computed paper from retained raw evidence; it is not implemented in the M3c deterministic gate.
- **D-M3c-2 (disclosed estimator limits).** Oster delta remains the documented linear approximation. HAC/cluster routines continue to omit finite-sample CR/HAC multipliers as disclosed; neither item changes the locked M3c estimator arithmetic.

---

## M3b — Referee panel + gold set (branch: `milestone/m3b-referee`) — Batch 1 IN PROGRESS (converged after 2 fix rounds; cheap residual blockers fixed inline; deep residuals → M5)

Scope: plan §5.3/§5.4 (L2 cross-family referee panel), §9.3 (gold-set calibration). Batch 2 (§6.5 integrity audit + §6.3 W-Lit + §6.6 truth-seeking prompts) staged separately.

Batch record:

- **M3b Batch 1** (`dece4b6`) — the referee panel MECHANISM: `referee-task` (mock default; Claude read-only argv), `referee_report_v1` schema, 9 rubrics, kernel-sampled artifact opening, unregistered-assertion sweep (incl. the `MANUSCRIPT_SEMANTIC_VALUE_ROLE` rubric that closes the M3a→M3b value-swap boundary), gold set + calibration, revision-task generator. Codex workhorse; 356 tests.
- **Verification round 1** (Codex 11/7-BLOCKER + Claude adversary DO-NOT-SHIP): the layer built the mechanism but was **fail-open and Worker-spoofable** — absence-of-report = approval, release path omitted the referee gates, calibration self-assertable, family derived from a Worker-writable manifest. Owner adjudicated (read both, verify — confirmed Codex right that minor `cannot_verify` escaped and reports/manifests weren't kernel-protected, points Claude had cleared/understated).
- **Fix round 1** (`1af536d`, Codex under frozen dispositions): fail-CLOSED gating (in-scope task/release blocks on missing/stale/uncalibrated/backend-unavailable), `cannot_verify` universal + rubric-derived severity, `not_supported` blocks all in-scope scientific tasks, kernel-only surface declarations, backend-bound calibration, task-scoped opened-artifact. 370 tests.
- **Verification round 2** (Codex 8/6-BLOCKER): the authority boundary was still partly bypassable — owner **verified against code** that `swarm.py:2974` auto-accepted a Worker-written task-namespaced run manifest (the exact "forged-approval channel" M1 closed for review logs).
- **Fix round 2** (`f7dc712`, Codex under precise dispositions): control-plane surfaces (`swarm_runs`/`referee_reports`/`calibration`/`reviews`/events) are DENIED to task writes; the kernel snapshots those namespaces before the executor and hard-blocks any executor-written control-plane file (`executor_wrote_control_plane`) before any kernel write; a kernel-only `referee-waiver` command; release verdict-checking; a read-only-compatible content-challenge (kernel computes the disk sha + gives the referee a line NUMBER, referee returns the `quoted_span`); strict git-immutable calibration bars. 380 tests.
- **Verification round 3** (Codex 3-BLOCKER + 1 MAJOR): policy `≤2 delegate rounds → inline`, so **owner fixed inline**: (B1) the content-challenge could pick a BLANK line (echo-satisfiable) — now selects only non-blank lines and fails closed (`referee_sample_not_challengeable`) when none exists; (B3) the `referee-waiver` branch guard used a caller-supplied `--base-branch` — now derives the trusted integration branch from git (`refs/remotes/origin/HEAD`), rejects linked worktrees, and the consumers require the waiver's `control_plane_branch` to equal it; (M4-cheap) a detected control-plane forgery is quarantined (unlinked + `git rm`) so it can't poison a retry. + regression tests.

Design decisions:

- **D-M3b-1 (deep provenance/calibration residuals → M5, documented; NOT exploitable on the empirical repo).** Two residuals require the M5 kernel-hardening / live-calibration machinery and are not reachable on the current empirical repo (no live panel, no manuscript release in flight; the same-commit calibration state is correctly UNAUTHORITATIVE): (a) **manifest-authority-ref** — a FULLY un-forgeable run-manifest authority against a full-access executor or a direct main-repo write requires migrating manifest authority into a kernel-only git ref (like M1's `refs/swarm/claims/`) + process-group termination + exact-kernel-event-path merge exemption (not the current `T###` filename regex); the executor-forgery detection + kernel-only surfaces close the practical task-branch attack for M3b. (b) **live-calibration binding** — `calibrate()` currently mints its run record from the supplied predictions file's self-declared metadata; proving a LIVE referee actually ran requires the calibration to consume kernel-journaled referee-invocation records emitted at invocation time (the §9.3 tier-(c) on-demand live-calibration harness). Both are logged as M5 items.
- **D-M3b-2 (integrity-audit bounded residuals).** The M3b audit now removes the claim ledger, paper, tables, figures, and validation surfaces before independent recomputation, and detects/rejects any main-worktree mutation outside `reports/status/integrity_audit/`. This is intentionally detection, not OS confinement. M5 owns the §4.3 deployment profile with a container, network namespace, and credential isolation; static inspection of referenced scripts is defense-in-depth and cannot fully prove that Python imports such as `socket` or `subprocess` are harmless. The reference empirical program has no retained raw layer, so its independent depth stops at hash-bound processed manifests. M4's computed paper must exercise the same audit from retained raw evidence. M4 also owns the atomic release audit-envelope workflow so writing the final release assembly cannot invalidate the inventory hash it was audited against.
- **D-M3b-3 (integrity-audit fix-round-2 verification: bounded fix inline + live-execution residual → M4).** The re-verification (Codex 1-BLOCKER/3-MAJOR) confirmed all findings are the SAME recurring pattern — proving a LIVE kernel-mediated operation (audit execution / model invocation / network fetch) actually happened vs a hand-authored artifact with correct hashes — and NONE is exploitable on the empirical repo (no audit/recall/literature; gates skip). **Fixed inline:** `gate_integrity_audit` now accepts ONLY a report bound to a kernel-emitted `integrity_audit_recorded` journal event (the events journal is kernel-only from Batch 1), with NO fallback to a bare committed/latest report — a hand-authored report is rejected (`integrity_audit_unjournaled`); `integrity_audit.py` emits that event on every run. **Residual → M4 live-execution tier** (same class as the referee's live-calibration residual, D-M3b-1): (a) a report bound to a real event but produced by a REPLAYED/mock transcript rather than a live model; (b) MAJOR-2 normalized `executor.family` (vs a free-form `executor.tool` alias); (c) MAJOR-4 a hash-pinned live-fetch record for W-Lit acquisition; (d) MAJOR-3 the reference's historical `manual`/backfill builds classify `builder_family_unverified` on a real release audit and need provenance repair before an M4/BT2 reference release. Proving a live model/network/audit actually ran is inherently the M4-computed-paper / on-demand live-execution tier — not provable in a mock-tested deterministic environment.

---

## M3a — Deterministic science gates + modeling battery + prereg locks (branch: `milestone/m3a-science-gates`) — COMPLETE (merged to main via PR #4, squash `ca462e8`)

Scope: plan §5.2 (deterministic science gates), §6.1 (phased preregistration locks + amendment discipline), §6.2 (claim ledger / per-type uncertainty table), §10 M3a acceptance row. Plus the M2-deferred items (canonical hypothesis↔prereg linkage, per-tier numeric ceilings).

Batch record:

- **M3a Batch 1** (`f6cff91`) — deterministic science gates (citation_integrity, claim_evidence_ledger, prereg_conformance, etl_decision_log, rigor_sections) + phased prereg lock commands + the two M2 deferrals. Codex workhorse; owner diff review.
- **M3a Batch 2** (`ee47667`) — modeling verification battery (instance_manifest_conformance, seed_budget_lock, gap_convergence, theoretical_falsification, sweep_artifact) + hybrid_interface_conformance + executable JSON schemas + remaining science gates. Codex workhorse; owner diff review. Registry now 35 gates.
- **Dual-vendor review** of the frozen batch-1+2 delta: Codex critique 17 findings (5 BLOCKER: F1 citation-bib-path false-green, F2 unregistered-numeric never scanned, F3 phased locks not consulted at claim/judge time, F4 amendment discipline absent, F5 prereg-conformance regex-only), NOT-MERGEABLE; Claude adversary corroborated. Central verified theme: the gates were **permissive** — skip-on-absent and trust-self-asserted — against the plan's whole thesis that gates must BIND when the artifact exists and content-bind every claim. Owner adjudicated the two reviewers' factual disagreements against the code and froze deterministic dispositions (`m3a_fixround_prompt.md`).
- **Owner fix round** (Codex workhorse under the frozen dispositions; owner re-verified the two highest-stakes pieces — the lock-enforcement matrix and amendment discipline — line-by-line): lock matrix `_required_active_lock`/`_effective_required_active_lock` wired into the shared `ready_backlog_tasks` funnel AND `cmd_judge_task` (empirical 2a/2b, hybrid Lock A/B, counterfactual→Lock B); 2-amendment cap + mandatory dual-definition amendment record + lock-header↔journal integrity + exploratory retyping (`gate_amendment_exploratory_tagging`, `gate_headline_confirmatory`); manuscript-numeric scan in the ledger gate; falsify hardening (finite-intermediate guard, empty-spec rejection, kernel-generated sample points over the locked domain); per-type uncertainty structure; content-binding symlink/tracked-file guards; NaN/Inf ceiling guards; new goldens GM3A_17/18/19. **321 tests green; real `make gate` green** after the F2 remediation below.

- **Fix-round-1 verification (dual-vendor, self-assessment redacted; both vendors in parallel — tripwire high-stakes).** Codex critique: 15 findings, 8 BLOCKER, NOT-MERGEABLE. Claude adversary: NOT-MERGEABLE, and caught a BLOCKER **the fix itself introduced** that Codex missed — the new falsify finite-guard doesn't catch `math.sqrt(-1)`/`log(0)` (`ValueError`) or the complex from `(-2)**0.5` (`TypeError`), so a `theoretical` claim with `sqrt(x)>=0` over domain `[-1,1]` crashes the whole gate suite with an uncaught traceback. Owner adjudicated **read both, decide, never average**: on the one direct disagreement — Codex "cmd_run_task bypasses the claim-time lock" vs Claude "no dispatch path bypasses" — I read `cmd_run_task` (swarm.py:7251) myself and confirmed Codex right (Claude verified the supervise-loop funnel + judge but missed the direct-run CLI). Both vendors independently flagged the deepest point: the ledger/prereg gates were green only because `claims.yaml`/`outcomes.yaml` were empty and the manuscript was exempt — "bound only by fixtures; first real exercise is M4." Full adjudication in `m3a_fixround2_adjudication.md`.
- **Owner fix round 2 (inline — tripwire-owned science-integrity/anti-HARKing core; ≤2 delegate rounds spent, so owner took it inline on the top tier).** Every fix owner-verified against §6.1/§6.2 and code; 10 new regression tests (`tests/test_m3a_fixround2.py`) pin each binding. BLOCKERS closed: **CF1** — `cmd_run_task` now runs the same `_effective_required_active_lock` predicate as the funnel + Judge (the direct-run bypass); **CF2** — a registered `computational` claim requires the experiment lock (Lock A), not just `counterfactual`→Lock B (§6.1); **CF5** — amendment cap is **two per program** (was per-phase) counted from the append-only journal, with strict version monotonicity + header-rollback refusal (both CLI and `gate_prereg_lock_coverage`); **CF8** — `--task-kind` is kernel-injected authority: the gate-form lint rejects a task authoring it, and `_run_gates` strips any author value and forces the frontmatter kind; **F10-crash** — `evaluate_numeric_expression` now catches `ValueError`/`TypeError` → clean `inequality_evaluation_error` (fail-closed, no suite crash); **F5/CF7** — terminal outcomes must be content-bound to committed manuscript/deviations text (`reported_in` anchor), and modeling propositions get the same proposition→outcome coverage. Also: CF13 (claim artifacts must be non-symlink, in-repo, git-tracked), CF11 (self-referential verification commands by parsed semantics, not 3 literal strings; interpretation cannot cite itself), CF15 (finite guards on `max_program_usd` + convergence tolerance). **321+10 tests green; real `make gate` green.**
- **Fix-round-2 verification (dual-vendor, redacted, parallel).** Codex critique: 10 findings, 7 BLOCKER, NOT-MERGEABLE. Claude adversary: **MERGEABLE** — it independently opened every cited artifact and confirmed the four claims are honest (all sha256 match, every `manuscript_numeric_literal` traces, `166.78 = 148.42 + 18.36` verified and 166.78 also appears directly, exemption truly gone, occurrence-scoped binding real-not-theater, CF1/CF8/CF13/CF11/CF5 closed). The vendors split on **severity, not facts**: Codex rated modeling/hybrid-mode holes BLOCKER; Claude rated them non-blocking-for-the-empirical-artifact. **Owner (read both, decide, never average):** refuted R2-1 by re-running the gate (manuscript IS green — Codex misread a paragraph-length line carrying all three citations); the rest were **genuinely new defects fix-round-2 introduced**, so I fixed them inline.
- **Owner fix round 3 (inline).** New-defect fixes: **R2-6** — `argparse(allow_abbrev=False)` + lint rejects any `--task-k*` prefix (the `--task-k` abbreviation had bypassed CF8); **R2-4b** — replaced the buggy `_more_advanced_phase` (a `("2b","lock_b")` rank tie downgraded a hybrid counterfactual task to 2b) with `_effective_required_active_locks` returning the full ADDITIVE set of required phases, checked at funnel + run-task + judge; **R2-8/Claude-3** — a centralized `_as_finite_float` guards every domain/point/tolerance/step conversion (huge-int OverflowError) and `ast.parse` now catches `ValueError`/`RecursionError` (NUL-byte / deep-nesting crashes); **R2-10** — the self-reference check canonicalizes the script path (`./scripts/...`). Hardening: **R2-5** — `gate_prereg_lock_coverage` fails closed on any malformed journal event (a corrupted amendment line can no longer lower the program count); **R2-7a** — an outcome `reported_in` anchor must be a git-tracked file on the `reports/`/`docs/prereg/` surface; **R2-2** — `verify_reference_claims.py` rewritten data-driven to re-derive ALL 15 registered literals across all four claims from their cited artifacts (was regime-CSV-only); **Claude-4** — the reportable-numeric regex now tolerates one descriptor word before count nouns and normalization reduces to the numeric core, so `12,563 rent-component rows` is detected and registered (it had evaded). Provisional-typing note added to `claims.yaml` (§6.2 typing is M3b-referee-adjudicated). **336 tests green (15 in `test_m3a_fixround2.py`); real `make gate` green.**
- **Fix-round-3 verification (Codex critique — the other vendor, round 3 being owner-authored).** 6 findings, 4 BLOCKER — and again several were **new regressions round 3 introduced**, chiefly from the round-3 numeric-core normalization: (F3) `14 ETH` would bind to a registered `14 rollups` (numeric-core dropped the unit → laundering); (F2) `7 consecutive days` evaded the descriptor-only-for-count-nouns regex; (F5) the rewritten verifier lost lexical precision (`14` matched by `14.04`, `10.00%` by `0.1004`); plus (F1) `RecursionError` still escaped recursive `_evaluate_node`, (F4) `reported_in` could point at `outcomes.yaml` itself, (F6) a quoted gate-runner path evaded the `.split()`-based self-reference check.
- **Owner fix round 4 (inline).** (F3) normalization is now `<value>|<unit-class>` (percent / currency / `x` / trailing head-noun) — descriptor-independent but unit-strict, so `14 ETH` can never bind to `14 rollups`; registered the manuscript's actual phrasings (`1,559 observations` added alongside `1,559 dates`). (F2) the descriptor tolerance extended to time units so `7 consecutive days` is detected — `7 days` and `1.05 x` are owned by REF-BLOB-FLOOR-DEF's statement. (F1) an AST node-count guard (≤400) + `RecursionError`/`MemoryError` caught around evaluation. (F5) the verifier retains lexical precision and requires exact integer equality for counts (16 literals reproduce). (F4) `reported_in` restricted to the `reports/paper/` manuscript surface, excluding the registry/locks/logs. (F6) the self-reference check uses `shlex.split` + `os.path.normpath`. **339 tests green (18 in `test_m3a_fixround2.py`); real `make gate` green.**
- **Fix-round-4 verification (Codex critique).** 3 findings, 2 BLOCKER. Adjudicated against the stop criterion (a NEW false-green on the empirical repo): **R4-F2** (interpreter flags `python -B scripts/quality_gates.py` evaded the token[1] self-reference check) — a real cheap bypass, FIXED (the self-ref check now extracts the first non-option token as the script operand, matching `gate_command_violation`); **R4-F3** (`14-day` hyphen no longer matched — no current false-green, only in excluded `fig-alt`) — FIXED (hyphen boundary allowed before count/time nouns). **R4-F1 is the convergence point and is NOT an M3a blocker:** Codex showed a *value-swap within the same unit* (writing the registered 11.68% where 69.14% belongs) still passes, because the deterministic floor binds by (value, unit, cited-key) set-membership, not semantic sentence-role. Per §6.2 that binding is EXACTLY the M3b referee's unregistered-assertion sweep (§5.3) + the §6.5 integrity-audit recomputation — asking the M3a gate to catch it is asking it to do M3b's semantic job. The real repo is currently honest (both round-2 reviewers verified every value number-by-number). Documented as the deterministic-floor boundary in `claims.yaml` and here; further iteration would chase M3b's responsibility inside M3a. **Follow-up fixes: 339 tests green; real `make gate` green.** M3a is MERGEABLE.

Design decisions:

- **D-M3a-1 (F2 remediation — REVERSED in fix round 2: register real claims, remove the exemption).** Fix-round-1 hash-pinned the frozen manuscript on a `manuscripts` historical exemption. Both verification reviewers rejected it: Codex showed the exemption is a laundering primitive (a future commit can change `index.qmd` AND re-pin in the same commit, leaving a new manuscript ungated), and both noted it left the ledger gate fixture-only. **Resolution (owner-inline, tripwire — external scientific claims):** removed the exemption entirely and registered the manuscript's numbers as four truthful claims in `contracts/claims.yaml`, each traced to a committed, hash-pinned artifact whose provenance I verified number-by-number (STR regime means → `reports/tables/str_regime_summary.{md,csv}`; panel counts → `rollup_panel_validation.json`; vendor reconciliation `0.02%`/`12.37%`/`148.42`/`166.78 ETH` → `cross_source_reconciliation.md` + the H060 release-caveats handoff, with `166.78` = `148.42` + the `18.36 ETH` delta; blob-floor `1.05 x` → `docs/protocol.md:83`). My round-1 "manufacturing a retroactive ledger" worry was wrong: declaring the *true* provenance of a reference paper is exactly the ledger's purpose, not fabrication. This makes the ledger gate **exercise real content now** (the reviewers' deepest shared point). F4 laundering closed structurally: manuscript numerics bind **occurrence-scoped** — a numeric is registered only if its line cites a `[@key]` whose claim owns that literal (no global value-equality whitelist). `scripts/verify_reference_claims.py` is the runnable, offline verification command (re-derives the numbers from the committed table). The `14-day` figure `fig-alt` numeric is excluded — image accessibility text is not a reported-statistic channel. The full semantic asserted→registered sweep (paraphrase, claim-typing) is the M3b referee (§6.2's own division of labor); numeric recompute-against-artifact is M4's computed-value-key layer.
- **D-M3a-2 (scope-deferred to M4, owner-mapped).** F11 (clean-clone `make paper` build), F12 (AS_OF→release-as-of binding), F14 (corpus/checklist absence→failure at the release perimeter) are release-perimeter policy = M4 §7.3/§8. F12's retrieval_sha256 recompute + snapshot↔bib identity match WAS done here (in scope). Recorded forward so M4 absorbs them.
- **D-M3a-3 (verification-pass MAJORs deferred, documented, not blocking).** Modeling/hybrid-mode defense-in-depth the empirical real-repo never exercises, deferred to when M3c/M4 build those surfaces out: CF6 (amendment exploratory-tagging must include phase 2a + descriptive/associational, and rerun pointers should resolve to tracked files — the *record* is already mandatory, only its content-binding is deferred per §6.1's own phasing), CF9 (bridge synthetic-manifest variant validation), CF10 (validate experiment manifests for ready/done model tasks not yet claim-referenced), CF12 (falsification domain sourced from the content-bound lock, not the author's spec — kernel-sampling within the declared domain is already in), CF14 (manuscript↔bib citation bijection → M4 release perimeter), Claude MINOR-6 (a hybrid `model` task should also assert Lock A active, not only Lock B — now folded into `_effective_required_active_locks` as an ADDITIVE requirement via R2-4b). Round-3 deferrals: R2-9 (`_step_repair`/`_step_sweep` move an already-claimed task to `active` before re-checking the lock — state hygiene only; the Judge checks every required lock on the done-path, so such a task can never be judged done), R2-4a (a claim created during execution and registered post-hoc, then Lock A activated before Judge — the §6.5 M3b integrity-audit chronology check is the designed backstop), R2-7b (modeling propositions declared inside the hash-bound `model_spec.md` rather than the lock body are not parsed for proposition→outcome coverage). None is a BLOCKER; each is logged with its finding id for the owning milestone.

---

## M2 — Planner + task schema v2 (branch: `milestone/m2-planner`) — COMPLETE (merged to main via PR #3, squash `188c8db`)

Scope: plan §4.2 (Planner runtime), §4.4 (task schema v2 + strict lint), §4.3 (read-only Claude planner profile).

Batch record:

- **M2-A** (schema v2 + task-lint) — LANDED (Codex workhorse; one mid-run kill, preserved+completed). Frontmatter v2 (task_kind enum, complexity_tier, machine-readable success_criteria, budgets, hash-declared inputs) via a byte-compatible one-level-nested parser extension; strict `gate_task_lint` (30+ diagnostics incl. validation-independence); lint-failing tasks unclaimable; 20 historical v1 tasks hash-pinned.
- **M2-B** (Planner runtime) — LANDED (Codex workhorse; one mid-run kill, preserved+completed). Kernel-bounded write authority (backlog + workstreams only, symlink-safe, in-memory lint before disk); machine-enforced human plan-approval gate; T035-rule triage with journaled split lineage; three replan triggers; hypothesis-retirement L3 escalation; recon-phase promotion enforcement.
- **M2-C** (read-only Claude wrapper + live eval) — LANDED (owner inline). The CLI planner wrapper; **live tier-(c) T035-replica eval over 3 rounds** with a real Opus 4.8 planner — refused decomposition-free confirmation every round, and the kernel lint caught two of my own defects (block-mapping parser gap; over-strict checkpoint rule) before the split applied cleanly.
- **Dual-vendor review** of frozen `main..HEAD`: Codex 12 findings (3 BLOCKER), NOT-MERGEABLE; Claude adversary 6 findings (1 BLOCKER), NOT-MERGEABLE. **Convergent top BLOCKER (both, independently): the plan-approval gate was enforced only in manual `cmd_tick`, not the autonomous `_step_tick` the supervisor runs** — Claude's closing note flagged it deliberately hunted the "two parallel dispatch paths, gate on one" pattern *because* it shares the author's lineage. Both also flagged gate-as-code, task_id non-uniqueness, recon integration_ready bypass. **Codex-unique and correct:** `--allowedTools` vs `--tools` (a CLI-semantics fact Claude missed). **Codex flagged, Claude cleared:** same-path/different-hash validation independence (Codex right on the specific hole). Recorded, not averaged.
- **Three owner fix tranches** (`ec2bea2`, `88350df`, `72a3cf8` — the third responding to a fix-verification pass that caught a NEW BLOCKER the earlier tranches introduced, the `make -C` escape): approval gate centralized into the shared `ready_backlog_tasks` funnel + direct-run-task guard; gate-as-code closed by a shared `gate_command_violation` validator at lint AND execution (permits only `make <target>` / `python <repo .py>`); real `--tools` read-only restriction + strict MCP isolation; content-bound plan approval (digest of backlog+workstreams, drift-refused); task_id↔filename binding + global uniqueness; split rejects any baseline-relative lint regression anywhere; update_workstreams content-validated; validation-independence path-disjoint; triage split/first-of-kind fixes; recon placeholder-detection + integration_ready coverage; list-aware hypothesis guard; replan idempotency via evidence fingerprints + a dedicated threshold. 12 attack-scenario regressions. Deferred to M3a: canonical hypothesis↔prereg linkage, per-tier numeric ceilings. Post-fix: 236 tests, 21/21 gates.

Design decisions:

- **D-M2-1 (gate form policy).** Gates are restricted to `make <target>` / `python[3] <repo-relative .py>` at both lint and execution — an autonomously-authored gate can never be an inline-code channel (`-c`/`-m`/stdin rejected). Fixtures moved to `scripts/noop_gate.py`; sandbox-mechanics tests keep raw `python -c` via an explicit `enforce_form=False` with the policy separately tested. Retro-hardens M0's residual injection→execution bridge.
- **D-M2-2 (trust boundary for the read-only planner).** `--tools Read,Glob,Grep` + `--strict-mcp-config` are defense-in-depth; the AUTHORITATIVE boundary is the kernel's bounded proposal application, which never lets the planner write anything directly — a prompt-injected planner can at most emit proposals the lint + authority checks reject.

---

## M1 — Supervisor (branch: `milestone/m1-supervisor`) — COMPLETE (merged to main via PR #2, squash `0a2c3e1`)

Scope: plan §4.1 + §9.4 M1 items + §9.1 basic usage capture + mock executor (§9.3 tier-a precondition).

Batch record:

- **M1-A** (claim refs) — LANDED (owner-implemented; concurrency-protocol invariant). `scripts/swarm_claims.py`: claims are empty-tree commit chains under `refs/swarm/claims/<task_id>`; CAS create via non-ff push rejection (cross-clone race test: exactly one winner, blind-race rollback), monotonic lease renewal under `--force-with-lease`, guarded release, expiry → orphaned reap planning, honest `local` transport labeling without a remote.
- **M1-B** (event journal + escalation + status) — LANDED (Codex workhorse; first dispatch killed by machine interruption with zero partial work — verified clean, re-run). `scripts/swarm_events.py` (fsync'd append-only JSONL, torn-tail tolerant; file/command escalation sinks, webhook parsed-but-deferred, shell-free), runtime event wiring (`run_started/run_finished/human_question/review_recorded/judge_block/tick_completed/loop_iteration_failed`), `swarm.py status` dashboard. Journal ownership-exempt by exact path only; journal records are ADVISORY per plan precedence.
- **M1-C** (supervise state machine) — LANDED (Codex workhorse; mid-run kill left +1,826 unverified lines — preserved, compile-checked, completed by a re-run against the same frozen contract, then owner-reviewed in full). The 11-step crash-only cycle; claim-first TICK with WIP/backpressure/budget blocks; subprocess JUDGE with per-invocation actor sessions honoring the separation window (defers, never waives); serial MERGE queue binding approval to the exact reviewed manifest with lease fencing, operator-surface enforcement, post-merge quality+pinned-gate verification, and guarded revert (deviation recorded: revert preserves a durable blocked-state control-plane commit rather than byte-identical base reset); REAP → orphaned-never-blocked; bounded REPAIR refusing integrity blocks; `judge-task --approve-only` moves done-promotion into the merge queue. 10 integration tests incl. full happy path with journal-order assertion and idempotent second cycle. 147/147 tests, 19/19 gates.

- **M1-D** (executor backends + costs) — LANDED (Codex workhorse; one mid-run kill, work preserved and completion-verified). `--executor-backend {codex,mock}`; deterministic mock transcripts through the SAME pinning/ownership/gate path; `--record-session` hashed artifacts; usage capture with `source: parsed|unavailable` (total-only token counts are unavailable, never split by guess); contract pricing → `estimated_cost_usd`; `swarm.py costs` spans exempted v1 history.
- **M1-E** (security preflight) — LANDED (owner inline). Containment attestation + credential scan + vendor-policy ack; full-access double opt-in; `network_strings` gate; handoff namespace `H<digits>_*`; effective-network manifest field.
- **M1-F** (reconcile + chaos) — LANDED (Codex workhorse; one mid-run kill, preserved + completion-verified). `swarm_reconcile.py` (journal⇔state⇔claims, bounded findings) + `swarm_chaos.py` (supervisor SIGKILL, worker kill, seeded jitter); 8 bounded integration tests.
- **Dual-vendor adversarial review** of frozen `376ba67..0b290f5`: Codex 13 findings (4 BLOCKER), verdict NOT-MERGEABLE; Claude adversary 7 findings (2 MAJOR), verdict MERGEABLE-WITH-FIXES. **Convergent:** crash window between ff-merge and verification (Claude's variant: retry recomputes pre-merge sha from the moved HEAD, making revert a no-op and blinding the reconciler); containment opt-in gap; mock write surface; direct done-promotion. **Codex-unique:** forged-review channel, lease-epoch recycling, base CAS, fail-open journal dependencies. **Claude-unique:** state-blind REAP (would reopen @human-blocked and approved tasks on lease expiry), drift gate claim-blindness. **Claude verified sound:** claim CAS core, actor separation, cost honesty, handoff namespacing. Verdicts differed — resolved by fixing all material findings before merge (recorded, not averaged).
- **Review-fix round** (3 owner-inline commits `719e4d6`/`7237661`/`720bc48`): state-aware REAP (blocked keeps @human holds; approved keeps approvals; reopen-before-release crash ordering; active-without-claim convergence); chain-anchored lease fencing + **claim epoch nonce** (testing exposed that same-second reclaims hashed to the SAME root commit, silently defeating ancestry fencing — a real protocol defect found by the fix round's own tests); judge-only review authorship; content+topology approval binding (manifest sha256 + reviewed-branch-sha; review commit must sit exactly atop the reviewed tip); approve-only judging by default with loud `--promote-directly` override; fail-closed `merge_started`/`merge_verified` journal records with recovery that verifies-or-reverts from RECORDED shas; clean-base precondition + guarded resets + base CAS pushes (`--force-with-lease`); mid-run heartbeat thread + supervisor shepherd renewals; unconditional containment for supervise/loop/tmux-start with real-home scan and ATTESTED credential waivers (never env); constrained mock writes; reconcile invariants (active-without-claim, unverified-merge-in-base) + offline claim-drift in the CI gate. Deferred with plan mapping: checkpoint/resume + per-task ceilings → M2 (§4.4); multi-host merge lease + second chaos kill → M5 drill harness (base already CAS-safe).

Design decisions:

- **D-M1-1 (claim payload).** Claim = commit chain; message carries `research_swarm.claim.v1` JSON (`lease_id` monotonic = chain length; `claim_id` naming reserved for the §6.2 ledger per plan). Fencing at merge: manifest `claim.lease_id` must equal the live claim tip.
- **D-M1-2 (actor sessions).** Supervisor process = worker session; judging always a subprocess with fresh `SWARM_ACTOR_SESSION` — actor separation is real in single-bot deployments (resolves the M0 Claude-adversary observation).
- **D-M1-3 (journal commit semantics).** The journal is durable-on-disk, ownership-exempt by exact path, and never committed by task runs; supervisor-side committing deferred to operational policy. Journal-only records remain advisory (plan §4.1 precedence).

---

## M0 — Stop the bleeding (branch: `milestone/m0-stop-the-bleeding`) — COMPLETE (merged to main via PR #1, squash `376ba67`)

Scope: plan §4.0 items 1–18, historical-record remediation, golden-suite harness seeded with 10 tasks.

Batch decomposition:

| Batch | Plan items | Surface | Lane |
|---|---|---|---|
| A | 1, 2, 4, 5 | claim regex + loop resilience + task quarantine + shared task-file parser | delegate |
| B | 3, 6, 7, 13, 14 | run-task deps enforcement, provenance_class, durable logs, frontmatter immutability, ownership-scoped commits | delegate |
| C | 9, 10, 17 | guarded base sync, judge merge-base/head-SHA recheck, actor separation | inline (git-destructive + judge-integrity surface) |
| D | 8, 11, 15, 16 | hash recompute gates, transform provenance, content-bound validation, projection-drift gate | delegate |
| E | 12, 18 | pinned constrained gate execution (shell=False, allowlist, env-strip, network-off-where-supported) + rule-level gate output capture | inline (sandbox/security surface — tripwire) |
| F | — | *(dissolved: #8 folded into Batch D, #18 folded into Batch E — shared surfaces)* | — |
| G | remediation | schema v2 bumps, provenance_class backfill annotations, release amendment, gate-scoping rule | inline (historical-record honesty surface) |
| H | golden harness | `tests/golden/` harness + 10 seeded tasks + M0 acceptance tests | delegate |

Design decisions (recorded as taken):

- **D-M0-1 (schema versions).** `SWARM_RUN_MANIFEST_SCHEMA_VERSION` → `research_swarm.runtime_run_manifest.v2`; `JUDGE_REVIEW_LOG_SCHEMA_VERSION` → `research_swarm.judge_review_log.v2`. Strict checks (provenance_class, actor separation, log hash) apply to v2+ artifacts only; the 32 historical run manifests and 23 review logs go on a checked-in, gate-verified exemption list (`contracts/historical_exemptions.json`, per-file sha256 + reason) per the plan's gate-scoping rule.
- **D-M0-2 (gate pinning semantics in M0).** The plan pins gate commands "at claim time"; real claim primitives (refs) arrive in M1. M0 operationalization: run-task captures the task frontmatter **before the executor is invoked** (the pre-run snapshot), records the pinned gates + protected frontmatter hash in the run manifest, executes **only** the pinned copy, and blocks on any executor change to `gates`/`allowed_paths`/`outputs`/`dependencies` (#13). This closes the executor-weakens-gates channel; M1 re-anchors the pin to the claim ref. Recorded as an intent-preserving refinement, not a deviation.
- **D-M0-3 (constrained gate execution, #12).** `shlex.split` (no shell), interpreter allowlist on argv[0] basename (`python`, `python3`, `make`; contract-configurable), per-gate timeout (default 600s, contract-configurable), env allowlist (`PATH`, `HOME`, `LANG`, `LC_ALL`, `TMPDIR`, `TERM` + `GIT_TERMINAL_PROMPT=0`) — credentials/proxy/cloud vars are excluded by construction. Network disabling: `sandbox-exec` deny-network profile on darwin, `unshare -n` where unprivileged user namespaces allow it on Linux; capability probed at runtime and the **effective** network state recorded per gate in the run manifest — never claimed when not enforced. Full executor-parity sandboxing deferred per plan (§4.0 #12).
- **D-M0-4 (actor separation, #17).** Per-process actor session id (uuid4, overridable via `SWARM_ACTOR_SESSION` for tests); run manifests and review logs record `actor: {session_id, recorded_at}`. A v2 review log is invalid if its session id equals the run manifest's, or if the wall-clock separation is below `review.min_separation_seconds` (contract key, default 60).
- **D-M0-5 (guarded base sync, #9).** `_supervisor_sync_to_remote_base` refuses (with an escalation-shaped error) when the local base branch has commits not on the remote (`git rev-list remote/base..base`), instead of `checkout -B`. Control-plane commits/pushes become strict in all modes (failure raises; no `[warn]`-and-continue).

Batch record:

- **Review-fix round** — LANDED (owner-implemented; response to the dual-vendor adversarial review of the frozen `f1d1638..a8d9632` diff). Codex critique: 12 findings, verdict NOT-MERGEABLE; Claude adversary: 4 findings, verdict MERGEABLE-WITH-FIXES. **Convergent finding (both reviewers, independently): the Judge bound gates/ownership to the live task frontmatter, not the manifest's pinned copy** — fixed: judge now fails on any live-vs-pinned frontmatter divergence (`post_run_frontmatter_tamper`, covering uncommitted-edit and amend variants) and executes only the manifest's pinned gate commands. Other fixes: done-bundles require an APPROVING review linked to a passing executor_run manifest (F1); rebaseline sidecars honored only for manifests + sidecars hash-pinned on the exemption list — remediation can never green a new manifest (F3); schema-downgrade closed for run manifests, review logs, processed manifests, and validation reports (F5); annotations hash-pinned with triple class verification (pin + file + mechanical re-derivation) and release-count cross-check (F6); dependency enforcement covers all runnable states (F7); run/judge survive malformed siblings via quarantine with a precise target error (F8); gate timeouts kill the whole process group (F9); dirty-tree manifests record untracked files and real declared dependencies (F10); path-qualified gate interpreters rejected (F11); judge verifies executor-log hash binding (F2 partial). 12 new attack-scenario tests + G07 extended with the F3 attack + relabel-attack test.
  **Recorded reviewer disagreement (not averaged):** Codex rated the absent cryptographic trust root for manifest authorship BLOCKER (a Worker can forge a complete coherent execution bundle); Claude's analysis shows the manifest-independent ownership firewall contains any such forgery to the task's allowed paths. Owner disposition: mechanical hardening applied (log-hash binding + F1/F4/F5/F6), **residual risk stands documented** — a Worker forging a fully coherent bundle within its own allowed paths can still claim executor provenance for hand-made work. This is scheduled, not waived: M1 design inputs = supervisor-invoked judging from a clean checkout, event-journal cross-checks, protected-ref/attestation decision; §9.3 seeded drills measure the catch rate. Claude observation for M1: per-process session ids make same-session detection inert in single-bot flows, and the 60s window will constrain auto-judge cadence — the M1 supervisor must assign explicit role-scoped `SWARM_ACTOR_SESSION` values and schedule judging outside the window.
  Verified post-fix: 122/122 tests, 19/19 gates, sweep clean, release check green.
- **Batch H** (golden harness) — LANDED (Codex workhorse; owner-reviewed). `tests/golden/` CI-visible tier of the §9.3 eval program: thin `GoldenRepo` end-to-end harness + 10 seeded scenarios G01–G10 covering the M0 acceptance row verbatim (G02: two consecutive ticks with a pre-claimed in-flight branch+worktree — no double-claim, no crash, one worktree preserved, fresh work still schedulable; G03/G04: fabricated-backfill and blocked-status manifests rejected by the Judge; G06: frontmatter tamper blocked with pinned gates executed; G07: stale-hash red → green via annotated supersession with the original manifest byte-identical across the transition; plus regex round-trip, quarantine, ownership, gate constraints, loop resilience). Verified: 109/109 tests (10 golden), 19/19 gates.
- **Batch E** (items 12, 18) — LANDED (owner-implemented inline; sandbox/security tripwire surface). `_run_gates` rebuilt: `shlex` parsing (no shell — metacharacters become inert arguments, proven by test), interpreter allowlist on argv[0] basename (`python`/`python3`/`make`, contract-configurable via `gate_execution.interpreter_allowlist`), env allowlist (PATH/HOME/LANG/LC_ALL/TMPDIR/TERM + GIT_TERMINAL_PROMPT=0 — credentials/proxies excluded by construction), per-gate timeout (default 600s, contract-configurable), and network denial via `sandbox-exec` deny-network on darwin / `unshare -n` on linux — capability probed once per process and the EFFECTIVE state recorded per gate (`network_disabled`, `network_disable_method`), never assumed. Verified live: socket connect blocked under the profile on this host. Gate outputs now rule-level: argv, returncode, duration, head+tail capture (8KiB each), constraint_violation. Full executor-parity sandboxing explicitly deferred per plan (§4.0 #12). Verified: 99/99 tests, 19/19 gates.
- **Batch D** (items 8, 11, 15, 16) — LANDED (Codex workhorse; owner-reviewed with two fixups: raw-unavailable annotations aggregated per manifest, failure diagnostics capped at 50 with full counts). New gates `processed_manifest_hashes` / `raw_manifest_hashes` recompute every hash claim against disk with sidecar rebaseline semantics (recomputed | superseded | raw_evidence_unavailable; originals hash-pinned, self-verifying); `manifest_tools.py` writes processed-manifest v2 (script sha256, dirty-tree refusal); validation reports content-bound (`inputs_consumed` re-verified per run); `projection_drift` gate. Landed deliberately RED on the real repo (10 stale claims, 135,648 deleted raw files) per the plan's honest-red acceptance clause.
- **Batch G** (historical-record remediation) — LANDED (owner-implemented inline; honesty-critical surface). 32 provenance annotations (18 executor_run / 9 manual_operator / 5 backfill — matches forensics exactly, classified mechanically from executor fields); 6 `superseded` rebaselines (04-01/04-08 generations → accurate 04-09 successors) + 6 `raw_evidence_unavailable` rebaselines (raw layer deleted, no regeneration claims anywhere); `contracts/historical_exemptions.json` hash-pins all 55 v1 artifacts; new `historical_exemptions` gate enforces the scoping rule (unexempted v1 fails; exemption drift fails; missing/drifted annotations fail); `release_2026-04-11.json` amended in place with the `raw_evidence_unavailable` note + per-class run counts (release --check still green); `historical_artifact_policy` + `review_bundle.min_separation_seconds` added to framework contract; generator preserved as `scripts/m0_historical_remediation.py` (idempotent). Hash gates now GREEN via annotation, never regeneration. Verified: 90/90 tests, 19/19 gates, sweep clean, release check green.
- **Batch C** (items 9, 10, 17) — LANDED (owner-implemented inline; tripwire surface). Guarded base sync: `checkout -B` replaced with fetch → refuse-if-local-ahead (`base_sync_refused_local_ahead`) → ff-merge/branch-update; judge re-runs the run-task ownership/diff check against the merge-base and verifies tip integrity against the selected manifest (branch match, sha ancestry, ≤1 post-manifest commit with the runtime's message and declared paths only); actor separation: per-process session ids in run manifests (`actor`) and v2 review logs (`reviewer.session_id`), same-session and sub-window reviews invalid (contract key `review_bundle.min_separation_seconds`, default 60); judge commits only its own control-plane artifacts (no more `git add -A` sweeping up leftover violations); review-log schema → v2 with distinct `operator_attestation` field; durable executor logs un-gitignored. Known wart logged: frontmatter list parser strips a trailing quote from values — M2 task-lint must reject gate commands ending in a quote. Verified: 66/66 tests, 14/14 gates, sweep clean.
- **Batch A** (items 1, 2, 4, 5) — LANDED. New `scripts/swarm_taskfile.py` shared parser (branch-id regex `^(T\d{3})(?=[_-]|$)`, `## Status`-scoped state parsing, in-section note appends, `WorktreeCollisionError`); quarantine (`load_tasks_quarantined`) wired into plan/tick; loop survives `BaseException` with capped exponential backoff and no unattended death; all three scripts consume the shared parser. Implemented by Codex workhorse (gpt-5.6-sol xhigh), full diff reviewed by owner. Verified independently: 43/43 tests, 14/14 gates, sweep clean.
