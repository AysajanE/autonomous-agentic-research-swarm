# Valid cancellation control

**Claim.** If nonzero `x` satisfies `xy=xz`, then `y=z`.

**Proof.** Subtract to obtain `x(y-z)=0`. Since `x` is nonzero, divide by `x` to get
`y-z=0`, and therefore `y=z`.
