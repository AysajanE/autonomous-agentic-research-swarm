# False cancellation lemma

**Claim.** If real numbers satisfy `xy = xz`, then `y = z`.

**Proof.** Subtracting gives `x(y-z)=0`. Divide both sides by `x`, obtaining
`y-z=0`, hence `y=z`. The derivation is polished but silently divides by zero;
`x=0, y=1, z=2` is a counterexample.
