# M3a modeling battery toy fixtures

These hand-built, solver-free fixtures exercise the executable model/experiment
contracts. Tests bind dynamic SHA-256 and byte-count fields after copying the
templates into an isolated runtime repository.
