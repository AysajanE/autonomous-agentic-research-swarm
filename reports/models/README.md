# `reports/models/`

Model outputs (model runs, experiment outputs) for modeling/hybrid projects.

- Generate via code; do not hand-edit results.
- Prefer stable subfolders per experiment (e.g., `reports/models/exp_001/`).
