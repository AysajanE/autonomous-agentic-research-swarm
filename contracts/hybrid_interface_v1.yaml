{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "research_swarm.hybrid_interface.v1",
  "title": "Executable empirical-to-modeling bridge interface",
  "description": "A bridge-generated instance manifest plus generation-time empirical validation evidence.",
  "allOf": [
    {
      "$ref": "schemas/instance_manifest_v1.json#/$defs/bridge"
    },
    {
      "required": [
        "pre_bridge_validation"
      ]
    }
  ]
}
